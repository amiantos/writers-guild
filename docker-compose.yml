services:
  writers-guild:
    build: .
    container_name: writers-guild-server
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./server/config.yaml:/app/config.yaml
    environment:
      - NODE_ENV=production
    restart: unless-stopped
