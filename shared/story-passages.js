/**
 * Enhanced Story Mode's record of a story's passages, shared by the server, which checks it, and
 * the client, which keeps it within bounds.
 */

/** The most passages a story's record holds. */
export const MAX_STORY_PASSAGES = 2000;
